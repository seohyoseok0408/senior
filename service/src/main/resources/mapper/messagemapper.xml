<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.repository.MessageRepository">

    <select id="selectOne" parameterType="Integer">
        SELECT *
        FROM message
        WHERE message_id = #{messageId}
    </select>

    <select id="findMessagesByReceiverId" parameterType="Integer">
        SELECT *
        FROM message
        WHERE receiver_id = #{receiverId}
        ORDER BY created_at DESC;
    </select>

    <select id="countUnreadMessages" parameterType="Integer">
        SELECT COUNT(*)
        FROM message
        WHERE receiver_id = #{receiverId}
          AND is_read = FALSE;
    </select>

    <update id="changeRead" parameterType="Integer">
        UPDATE message
        SET is_read = TRUE
        WHERE message_id = #{messageId}
    </update>

    <insert id="insert" parameterType="Message">
        INSERT INTO message (receiver_id, title, content, is_read)
        VALUES (#{receiverId}, #{title}, #{content}, #{isRead})
    </insert>

</mapper>